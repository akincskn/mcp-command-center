import { ComingSoon } from '@/components/shared/ComingSoon';

export default function SettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      description="Configure your default agent tier, manage API keys, and customize tool preferences."
      phase="Phase 12"
    />
  );
}
